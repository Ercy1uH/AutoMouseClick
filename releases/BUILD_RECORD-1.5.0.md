# MouseClik 1.5.0 构建记录

| 项目 | 结果 |
|---|---|
| 构建日期 | 2026-09-21（Asia/Shanghai） |
| 源码输入提交 | `8492f2a` |
| 平台 | Windows x64 |
| 构建命令 | `npm run dist` |
| 构建工具 | electron-builder 26.15.3，Electron 38.8.6 |
| 产物 | `MouseClik-1.5.0-portable.exe` |
| 文件大小 | 85,259,247 字节 |
| SHA-256 | `a3f7e2dc399f9fa71133a18d360190c0a1a83abe0a5ae5310846c09a7974ba8c` |
| 数字签名 | 未签名 |

## 验证结果

- 105 项单元与基础设施回归测试通过，Windows PowerShell 执行器测试通过。
- 5 个界面套件通过：配置编辑与悬浮条、循环预览、循环编辑、基础操作、步骤设置。
- HTTP、持久化与重启恢复、Electron 桌面交互套件通过。
- 使用 7-Zip 从上述 EXE 中提取 `app-64.7z` 并解包；14 个应用源码文件均与源码输入逐字节一致，`package.json` 的版本与入口匹配。
- 提取后的 `resources/app/` 文件清单与 `build.files` 一致，无额外应用数据或日志文件。
- 直接用 EXE 中提取的 `MouseClik.exe` 运行桌面套件，通过启动、预览加点、悬浮条和暂停/继续控制链路检查。
- 测试结束后确认进程和端口释放，临时配置清理，本机应用数据文件保持原样。

本记录及发布说明的后续更新不在 `build.files` 内，不改变上述产物。重新打包后应重新记录大小和哈希。

## 验证边界

桌面控制测试对执行请求使用受控响应，并不证明所有外部应用的真实点击行为。未完成不同权限、100%/125%/150% 系统缩放下的真实目标窗口点击矩阵；界面缩放检查不等于此项验证。

历史记录保存在 [archive/BUILD_RECORD-1.4.2.md](archive/BUILD_RECORD-1.4.2.md)。
